import { StyleSheet } from "react-native";

const HeaderStyle = StyleSheet.create({
    text: {
        fontSize: 32,
        fontWeight: "bold",
        textAlign: "center"
    }
});

export default HeaderStyle;